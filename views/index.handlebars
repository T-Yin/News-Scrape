<div class="container">

    <div class="jumbotron text-center">
        <h1 class="display-4"><i class="far fa-newspaper"></i>

            <b>News Scrape</b></h1>
        <p class="lead">This website scrapes the news articles from CNN's main stories.</p>
        <hr class="my-4">
        <a class="btn btn-success btn-lg" href="/scrape" role="button">Start Scrape</a>
    </div>

    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col-sm-10">Article Name</th>
                <th scope="col-sm-1">Link</th>
                <th scope="col-sm-1">Note</th>
            </tr>
        </thead>
        <tbody id="articles">

        </tbody>
    </table>

    {{!-- <div id="wrapper">
        <div id="articles"></div>
        <div id="notes"></div>
    </div> --}}

    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div id="notes">


                </div>
            </div>
        </div>
    </div>

</div>